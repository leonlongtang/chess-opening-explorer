[project]
name = "chess-opening-explorer"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "lnikolop", email = "leonlongtangnik@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "chess>=1.11.2",
    "fastapi>=0.141.1",
    "pandas>=3.0.6",
    "pyarrow>=25.0.1",
    "uvicorn>=0.53.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
